
<% @items.each do |item| %>
<%= item.title %><br>
<%= item.description %><br>
Price: <%= number_to_currency(item.price) %><br>
<img src=" <%= item.image_path %> " style="width:304px;height:228px;"  /><br><br>
<%= button_to "Add to Cart", cart_items_path({item_id: item.id}) %>
<% end %>
